{
  "filename": "MapMethodsExample.java",
  "content": "/**\n * MapMethodsExample.java\n * \n * This Java program demonstrates essential methods of the Map interface,\n * especially focusing on keySet(), values(), entrySet(), and utility methods like size(), isEmpty(), containsKey().\n * It helps beginners understand how to access and iterate over map data.\n */\n\nimport java.util.HashMap;\nimport java.util.Map;\nimport java.util.Set;\nimport java.util.Collection;\n\npublic class MapMethodsExample {\n\n    public static void main(String[] args) {\n        // Create a HashMap to store students and their scores\n        HashMap<String, Integer> scores = new HashMap<>();\n\n        // Add some entries (student names as keys, scores as values)\n        scores.put(\"Alice\", 95);\n        scores.put(\"Bob\", 87);\n        scores.put(\"Charlie\", 92);\n        scores.put(\"Diana\", 88);\n\n        // Retrieve all keys (student names) using keySet()\n        Set<String> students = scores.keySet();\n        System.out.println(\"Students: \" + students);\n\n        // Retrieve all values (scores) using values()\n        Collection<Integer> allScores = scores.values();\n        System.out.println(\"All scores: \" + allScores);\n\n        // Retrieve all entries (pairs of student and score) using entrySet()\n        Set<Map.Entry<String, Integer>> entries = scores.entrySet();\n        System.out.println(\"Complete entries: \" + entries);\n\n        // Use utility methods to get map size, check if empty, and check keys/values\n        System.out.println(\"Number of students: \" + scores.size()); // Counts entries\n        System.out.println(\"Is map empty? \" + scores.isEmpty()); // Checks if map has no entries\n        System.out.println(\"Does Alice exist? \" + scores.containsKey(\"Alice\")); // Checks for specific key\n        System.out.println(\"Is score 95 present? \" + scores.containsValue(95)); // Checks for specific value\n    }\n}\n"
}